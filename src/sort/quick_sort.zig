const std = @import("std");

/// Quick sort is a partition based
/// algorithim ...
/// @param { []i64 } array -> array to sort on
/// @returns { void }
pub fn quick_sort(array: []i64) void {
    var pivot = array.len - 1;

    
}
